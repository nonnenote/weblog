<div class="container">

<header class="header">ブログ一覧</header>
<p><%= notice %></p>

<article>

 <div class="contents">
<% @blogs.each do |blog| %>
  <div>
    <div class="title">タイトル:<%= blog.title %><br>内容:<%= blog.content %><br>
    <%= link_to '詳細を確認する', blog_path(blog.id) %>
    <%= link_to "ブログを編集する", edit_blog_path(blog.id) %>
    <%= link_to 'ブログを削除する', blog_path(blog.id), method: :delete, data: { confirm: '本当に削除していいですか？' } %></div>
<% end %>
  </div>
  </div>
  <br>
  <div class="topic">
        <%= link_to '新しくブログを投稿する', new_blog_path %>
  </div>

</article>
</div>
